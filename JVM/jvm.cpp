#include "jvm.hpp"

void JVM::initialize(string classPath) {
  //this->methodArea.loadClass(classPath);
  //this->frameStack.pushFrame(this->methodArea.getMainMethod());
}

void JVM::run() {
  // while (this->frameStack.getStackSize() > 0) {
  //   this->frameStack.executeFrame();
  // }
}